# 信号处理模块

所有物理信号均以16位有符号整数表示，范围为-32768至32767，实际大小及单位和板卡输出振幅挂钩。

参数设定格式：
<模块变量名>.write(<地址/参数名>, <值>)

```python
pid = module.ModulePID(bus_inst, "PIDC")
pid.write(0, 16384)
pid.write("gain_p", 16384)  
pid.write("saturation_gain", 10) # 10dB
```

参数读取格式：
<模块变量名>.read(<地址/参数名>)

```python
...
pid.read("gain_p") # 16384
pid.read("saturation_gain") # 10
```

部分间接参数在读取时可能返回无穷大np.inf。

部分位宽为1的参数，仅在上升沿或下降沿有效，对应格式：
<模块变量名>.flip_on(<地址/参数名>)
<模块变量名>.flip_off(<地址/参数名>)

```python
sclofsm = module.ModuleSCLOFSM(bus_inst, "SCLO")
sclofsm.flip_on("lock") # 在lock参数产生一个上升沿
sclofsm.flip_off("lock") # 在lock参数产生一个下降沿
```

## PID控制器

### 基本信息

数量：2

类名：ModulePID

### 端口

输入：
- 自动开关指令（1bit布尔值）
- 误差信号输入（16bit物理信号）

输出：
- 控制信号输出（16bit物理信号）

端口名：
|标识符|自动开关指令|误差信号输入|控制信号输出|
|----|----|----|----|
|PIDC|PID_RESET|PID_IN|PID_OUT|
|PID2|PID2_RESET|PID2_IN|PID2_OUT|


### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|gain_p|24|-2^23 ~ 2^23-1|P通道增益系数|线性增益系数。取对数后才可转换为分贝值。可通过间接参数直接设定分贝值|
|1|gain_i|32|-2^31 ~ 2^31-1|I通道增益系数|同上|
|2|gain_d|24|-2^23 ~ 2^23-1|D通道增益系数|同上|
|3|setpoint|16|-32768 ~ 32767|设定值|物理信号|
|4|limit_integral|16|0 ~ 32767|积分限幅值|物理信号|
|5|limit_sum|16|0~ 32767|输出限幅值|物理信号|
|6|leak_digit|32|无效|积分泄漏控制参数|仅推荐通过间接参数修改
|7|enable_auto_reset|1|0 / 1|自动控制使能|设为1时，模块接受其它状态机模块的开关指令。设为0时，开关指令被忽略|

### 间接参数

|参数名|范围|含义|备注|
|----|----|----|----|
|overall_gain|实数，单位为dB|整体增益|调节P通道增益，并等比例缩放I、D通道增益。若缩放后任一通道增益超出范围则报错。若初始P通道增益为0（缩放比例为无穷大），则仅调节P通道增益|
|pi_corner|非负实数，单位为Hz|PI交点频率|调节I通道增益。使得PI增益曲线交点位于指定频率。若调节后I通道增益超出范围则报错|
|pd_corner|非负实数，单位为Hz|PD交点频率|调节D通道增益。使得PD增益曲线交点位于指定频率。若调节后D通道增益超出范围则报错|
|saturation_gain|实数，单位为dB|饱和增益|调节泄漏系数，使得积分通道在设定增益处饱和。若饱和增益过大，则设定为无饱和。若饱和增益过小，则设定为最大泄漏。若I通道增益为0，则报错|
|saturation_turning_frequency|非负实数，单位为Hz|饱和拐点频率|调节泄漏系数，使得积分通道在设定频率处饱和。若饱和频率过小或为0，则设定为无饱和。若饱和频率过大，则设定为最大泄漏|

### 可视化

在自由模式下，允许用户调整除了leak_digit以外的所有直接和间接参数。

绘制PID控制器的增益曲线：
- 一个坐标系，横坐标为对数频率（Hz），纵坐标为增益（dB）。
- 根据overall_gain绘制比例通道的响应，斜率为0，截距即为overall_gain。
- 根据pi_corner绘制积分通道的响应，斜率为-20dB/十倍频程，在pi_corner处与比例通道响应曲线相交。
- 根据pd_corner绘制微分通道的响应，斜率为+20dB/十倍频程，在pd_corner处与比例通道响应曲线相交。
- 根据saturation_turning_frequency绘制积分饱和响应，斜率为0，在saturation_turning_frequency处与积分通道响应曲线相交。
- 用户可以通过调整参数，观察增益曲线的变化。
- 注意根据gain_p、gain_i、gain_d的实际值，上述参数可能出现0或者无穷大的特殊情况，绘图时需要进行相应处理。

## 累加器

### 基本信息

数量：2

类名：ModuleAccumulator

### 端口

输入：
- 外部反馈输入（16bit物理信号）
- 自动开关指令（1bit布尔值）
- 暂停累加指令（1bit布尔值）

输出：
- 慢变累加值输出（16bit物理信号）
- 快变累加值输出（16bit物理信号）

端口名：
|标识符|外部反馈输入|自动开关指令|暂停累加指令|慢变累加值输出|快变累加值输出|
|----|----|----|----|----|----|
|ACCM|ACC_IN|ACC_RESET|ACC_PAUSE|ACC_OUT|ACC_FAST_OUT|
|ACC2|ACC2_IN|ACC2_RESET|ACC2_PAUSE|ACC2_OUT|ACC2_FAST_OUT|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|low|32|无效|每周期慢变累加值低32位|与high组成64位累加值|
|1|high|32|无效|每周期慢变累加值高32位|仅推荐通过间接参数修改|
|2|divisor|16|无效|快变累加值与慢变累加值的频率之比|仅接受2的幂次，从高位起依次检测每位是否为1。仅推荐通过间接参数修改|
|3|/|/|/|/|保留|
|4|lf_kp|24|-2^23 ~ 2^23-1|环路滤波器P通道增益系数|线性增益系数。取对数后才可转换为分贝值。涉及锁相环性能，不推荐修改|
|5|lf_ki|32|-2^31 ~ 2^31-1|环路滤波器I通道增益系数|同上|
|6|bypass_lf|1|0 / 1|环路滤波器略过|设为1时，环路滤波器被略过，外部反馈值直接加到每周期累加值上。设为0时，外部反馈经过环路滤波器|
|7|enable_auto_reset|1|0 / 1|自动控制使能|设为1时，模块接受其它状态机模块的开关指令。设为0时，开关指令被忽略|

### 间接参数

|参数名|范围|含义|备注|
|----|----|----|----|
|freq|0~125M闭区间内的实数，单位为Hz|频率|调节慢变累加器的频率，使得快变累加器输出指定频率的锯齿波|
|ratio|2的0~15次幂|频率比|同时调节慢变累加器的频率和divisor，使得在快变累加器频率不变的前提下达到指定频率比|

### 可视化

在自由模式下，允许用户调整enable_auto_reset、bypass_lf、freq与ratio参数。

绘制累加器输出波形示意图，即两路锯齿波。

## 三角函数运算器

### 基本信息

数量：2

类名：ModuleBase（无专用类）

### 端口

输入：
- 相位信号输入（16bit物理信号，±32768视作±π）

输出：
- 正弦信号输出（16bit物理信号）
- 余弦信号输出（16bit物理信号）

端口名：
|标识符|相位信号输入|正弦信号输出|余弦信号输出|
|----|----|----|----|
|TRIG|TRI_IN|TRI_SIN|TRI_COS|
|TRI2|TRI2_IN|TRI2_SIN|TRI2_COS|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|/|/|/|/|保留|
|1|/|/|/|/|保留|

### 间接参数

无

### 可视化

无

## 反三角函数运算器

### 基本信息

数量：2

类名：ModuleBase（无专用类）

### 端口

输入：
- 正弦信号输入（16bit物理信号）
- 余弦信号输入（16bit物理信号）

输出：
- 相位信号输出（16bit物理信号，±32768视作±π）

端口名：
|标识符|正弦信号输入|余弦信号输入|相位信号输出|
|----|----|----|----|
|ATAN|ATAN_SIN|ATAN_COS|ATAN_OUT|
|ATA2|ATAN2_SIN|ATAN2_COS|ATAN2_OUT|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|/|/|/|/|保留|
|1|/|/|/|/|保留|

### 间接参数

无

### 可视化

无

## 线性缩放器

### 基本信息

数量：4

类名：ModuleScaler

### 端口

输入：
- 信号输入（16bit物理信号）

输出
- 信号输出（16bit物理信号）

端口名：
|标识符|信号输入|信号输出|
|----|----|----|
|SCLR|SCALER_IN|SCALER_OUT|
|SCL2|SCALER2_IN|SCALER2_OUT|
|SCL3|SCALER3_IN|SCALER3_OUT|
|SCL4|SCALER4_IN|SCALER4_OUT|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|scale|24|-2^23 ~ 2^23-1|缩放系数|线性增益系数。取对数后才可转换为分贝值。可通过间接参数直接设定分贝值|
|1|bias|16|-32768 ~ 32767|偏置量|物理信号|
|2|upper_limit|16|-32768 ~ 32767|输出上限|物理信号|
|3|lower_limit|16|-32768 ~ 32767|输出下限|物理信号|
|4|enable_wrapping|1|0 / 1|环绕使能|设为1时，输出信号无视上下限，并在达到16位物理信号最大表示范围时环绕。设为0时，输出信号受上下限限制|

### 间接参数

|参数名|范围|含义|备注|
|----|----|----|----|
|gain|实数，单位为dB|增益|调节缩放系数。若所要求系数超出范围则报错|

### 可视化

在自由模式下，允许用户调整除了enable_wrapping以外的所有直接和间接参数。

## FIR滤波器

### 基本信息

数量：4

类名：ModuleFIRFilter

### 端口

输入：
- 信号输入（16bit物理信号）

输出：
- 信号输出（16bit物理信号）

端口名：
|标识符|信号输入|信号输出|
|----|----|----|
|FIRF|FIR_IN|FIR_OUT|
|FIR2|FIR2_IN|FIR2_OUT|
|FIR3|FIR3_IN|FIR3_OUT|
|FIR4|FIR4_IN|FIR4_OUT|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0-63|coef_0 ~ coef_63|24|-2^23 ~ 2^23-1|滤波器系数|滤波器的冲激响应，共64个抽头，仅推荐通过专用方法修改|
|64|norm|18|0 ~ 262143|归一化系数|用于归一化滤波器输出，仅推荐通过专用方法修改|

### 间接参数

无

### 特殊方法

方法design_lowpass用于设计一个FIR低通滤波器并立即载入生效。

定义：
```python
def design_lowpass(self, freq_pass, freq_stop, freq_sample, weight = 1):
```

参数说明：
- freq_pass：通带截止频率，单位为Hz
- freq_stop：阻带截止频率，单位为Hz
- freq_sample：采样频率，单位为Hz，默认为250MHz，实际取决于硬件型号
- weight：阻带权重，默认为1

用法：
```python
fir = module.ModuleFIRFilter(bus_inst, "FIRF")
fir.design_lowpass(1e6, 10e6, 250e6) # 设计一个通带截止频率为1MHz，阻带截止频率为10MHz的低通滤波器
```

### 可视化

一个简易的滤波器设计工具：
- 参考matlab中的滤波器设计工具，保留核心功能
- 允许通过工具输入参数
- 绘制滤波器的频率响应曲线
- 绘制滤波器的相位响应曲线
- 绘制滤波器的冲激响应
- 绘制滤波器的零极点图

## 线性变换器

### 基本信息

数量：2

类名：ModuleLinerTransformer

### 端口

输入：
- 信号A输入（16bit物理信号）
- 信号B输入（16bit物理信号）

输出：
- 信号A输出（16bit物理信号）
- 信号B输出（16bit物理信号）

端口名：
|标识符|信号A输入|信号B输入|信号A输出|信号B输出|
|----|----|----|----|----|
|LTRN|LN_TRANSFORMER_IN_A|LN_TRANSFORMER_IN_B|LN_TRANSFORMER_OUT_A|LN_TRANSFORMER_OUT_B|
|LTR2|LN_TRANSFORMER2_IN_A|LN_TRANSFORMER2_IN_B|LN_TRANSFORMER2_OUT_A|LN_TRANSFORMER2_OUT_B|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|coef_aa|16|-32768 ~ 32767|变换矩阵元素AA|输出A对输入A的线性变换系数，仅推荐通过间接参数修改|
|1|coef_ab|16|-32768 ~ 32767|变换矩阵元素AB|输出A对输入B的线性变换系数，仅推荐通过间接参数修改|
|2|coef_ba|16|-32768 ~ 32767|变换矩阵元素BA|输出B对输入A的线性变换系数，仅推荐通过间接参数修改|
|3|coef_bb|16|-32768 ~ 32767|变换矩阵元素BB|输出B对输入B的线性变换系数，仅推荐通过间接参数修改|

### 间接参数

|参数名|范围|含义|备注|
|----|----|----|----|
|a00|-1.0 ~ 1.0内的实数|变换矩阵元素00|若线性变换后结果超出16位物理信号范围则自动饱和|
|a01|-1.0 ~ 1.0内的实数|变换矩阵元素01|/|
|a10|-1.0 ~ 1.0内的实数|变换矩阵元素10|/|
|a11|-1.0 ~ 1.0内的实数|变换矩阵元素11|/|
|matrix|2x2实数矩阵|变换矩阵|以二维列表形式传入，即[[a00, a01], [a10, a11]]，对系数进行L1意义上的归一化以阻止饱和|

### 可视化

以2x2矩阵形式显示当前变换矩阵，及各个输入/输出分别对应哪一行/列。

## 混频器

### 基本信息

数量：4

类名：ModuleBase（无专用类）

### 端口

输入：
- 信号A输入（16bit物理信号）
- 信号B输入（16bit物理信号）

输出：
- 混频信号输出（16bit物理信号）

端口名：
|标识符|信号A输入|信号B输入|混频信号输出|
|----|----|----|----|
|MIXR|MIXER_IN_A|MIXER_IN_B|MIXER_OUT|
|MIX2|MIXER2_IN_A|MIXER2_IN_B|MIXER2_OUT|
|MIX3|MIXER3_IN_A|MIXER3_IN_B|MIXER3_OUT|
|MIX4|MIXER4_IN_A|MIXER4_IN_B|MIXER4_OUT|

### 直接参数

无

### 间接参数

无

### 可视化

无

## 解卷绕器

### 基本信息

数量：1

类名：ModuleBase（无专用类）

### 端口

输入：
- 相位信号输入（16bit物理信号，±32768视作±π）

输出：
- 解卷绕相位信号输出（16bit物理信号，在32767与-32768饱和）

端口名：
|标识符|相位信号输入|解卷绕相位信号输出|
|----|----|----|
|UNWR|UNWRAPPER_IN|UNWRAPPER_OUT|

### 直接参数

无

### 间接参数

无

### 可视化

展示解卷绕操作的示意图。（详细沟通）

## PDH状态机

### 基本信息

数量：1

类名：ModulePDHFSM

### 端口

输入：
- 透射光强信号输入（16bit物理信号）
- 锯齿波扫描输入（16bit物理信号）

输出：
- PID复位信号（1bit布尔值）
- 混频器复位信号（1bit布尔值）
- 扫描复位信号（1bit布尔值）

端口名：
|标识符|透射光强信号输入|锯齿波扫描输入|PID复位信号|混频器复位信号|扫描复位信号|
|----|----|----|----|----|----|
|PDH|PDHFSM_IN_POWER|PDHFSM_IN_SCAN|PDHFSM_PID_RESET_CTRL|PDH_FSM_MIXER_RESET_CTRL|PDHFSM_SCAN_RESET_CTRL|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|pc_cmd|2|0 ~ 3|工作模式指令|00:空闲/复位, 01:手动扫描, 10:自动校准, 11:自动锁定退出|
|1|threshold_signal_locking|16|-32768 ~ 32767|手动锁定阈值|物理信号。退出锁定状态的振幅阈值|
|2|threshold_signal_scanning|16|-32768 ~ 32767|手动扫描阈值|物理信号。从扫描状态切换为锁定状态的振幅阈值|
|3|time_duration_scanning|32|0 ~ 2^32-1|手动扫描确认时间|单位为时钟周期。从扫描状态切换为锁定状态的时间阈值|
|4|time_duration_locking|32|0 ~ 2^32-1|手动锁定超时时间|单位为时钟周期。退出锁定状态的时间阈值|
|5|coef_scan|16|-32768 ~ 32767|自动扫描系数|Q1.15格式定点数。用于自动计算扫描阈值比例，32767为1.0，16384为0.5|
|6|coef_lock|16|-32768 ~ 32767|自动锁定系数|Q1.15格式定点数。用于自动计算锁定阈值比例，同上|

### 间接参数

无

### 可视化

在手动模式下，允许用户调整所有直接参数。
- 手动模式下，绘制各阈值物理含义的示意图。（详细沟通，可能需同步到示波器）
- 自动模式下，绘制设定比例系数的示意图。（详细沟通）
- 显示当前状态机的状态，如IDLE, SCANNING, LOCKING等。（架构上不可能，待将来更新）

## LO自动校准状态机

### 基本信息

数量：1

类名：ModuleSCLOFSM

### 端口

输入：
- 剩余相位输入（16bit物理信号）

输出：
- 相位校准值输出（16bit物理信号）
- PID复位信号（1bit布尔值）

端口名：
|标识符|剩余相位输入|相位校准值输出|PID复位信号|
|----|----|----|----|
|SCLO|SCLOFSM_PHASE_IN|SCLOFSM_BIAS_OUT|SCLOFSM_PID_RESET_CTRL|

### 直接参数

|地址|参数名|位宽|范围|含义|备注|
|----|----|----|----|----|----|
|0|lock|1|0 / 1|锁定指令|上升沿时，更新校准值，打开PID。下降沿时，关闭PID，但校准值维持|
|1|clear|1|0 / 1|清除指令|上升沿时，清除校准值为0|

### 间接参数

无

### 可视化

无

# 封装后模块

由一个或多个基础模块封装而成。

端口连接格式：

router.set_routing(<目标端口名>, <源端口名>)

router.upload()

```python
router.set_routing("TRI_IN", "ACC_OUT")
router.set_routing("OUTPUT_A", "TRI_SIN")
router.upload() # 一次同时上传所有连接设定
```

特殊端口：
- VOID：通往该端口的16bit物理信号被忽略，来自该端口的信号恒为0，用于表示“断开”状态。
- VOID_BOOL：同上，但适用于布尔信号。
- LOW：来自该端口的布尔信号恒为0。
- HIGH：来自该端口的布尔信号恒为1。

## 正弦波发生器

- 累加器 x 1
- 三角函数运算器 x 1

### 端口

输入：
- 自动开关指令（1bit布尔值）

输出：
- 正弦信号输出（16bit物理信号）
- 余弦信号输出（16bit物理信号）

### 连接方式

累加器：
- 外部反馈输入：断开
- 自动开关指令：模块自动开关指令
- 暂停累加指令：连接至LOW
- 累加值输出：连接至三角函数运算器相位信号输入
- 快变累加值输出：断开

三角函数运算器：
- 相位信号输入：连接至累加器累加值输出
- 正弦信号输出：模块正弦信号输出
- 余弦信号输出：模块余弦信号输出

### 可用参数

累加器：
- freq
- enable_auto_reset

### 初始化

调用各内部模块初始化函数，断开所有外部端口连接。

### 可视化

待补充。

## 数字控制振荡器

- 累加器 x 1
- 三角函数运算器 x 2

### 端口

输入：
- 外部频率调制（16bit物理信号）
- 自动开关指令（1bit布尔值）

输出：
- 正弦信号输出（16bit物理信号）
- 余弦信号输出（16bit物理信号）
- 倍频正弦信号输出（16bit物理信号）
- 倍频余弦信号输出（16bit物理信号）

### 连接方式

累加器：
- 外部反馈输入：模块外部频率调制
- 自动开关指令：模块自动开关指令
- 暂停累加指令：连接至LOW
- 累加值输出：连接至第一个三角函数运算器的相位信号输入
- 快变累加值输出：连接至第二个三角函数运算器的相位信号输入

三角函数运算器1：
- 相位信号输入：连接至累加器累加值输出
- 正弦信号输出：模块正弦信号输出
- 余弦信号输出：模块余弦信号输出

三角函数运算器2：
- 相位信号输入：连接至累加器快变累加值输出
- 正弦信号输出：模块倍频正弦信号输出
- 余弦信号输出：模块倍频余弦信号输出

### 可用参数

累加器：
- freq
- enable_auto_reset
- ratio

### 初始化

调用各内部模块初始化函数，断开所有外部端口连接。
累加器的bypass_lf设定为0。

### 可视化

待补充。

